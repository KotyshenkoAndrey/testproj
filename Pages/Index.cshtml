@page
@model testproj.Configuration.IndexModel
@{
    ViewData["Title"] = "API";
}

<div class="stamp"></div>

<div class="text-left m-3">
    <h1>@(Model.HelloMessage ?? "API")</h1>
    <div class="mt-3">

        @if (Model.OpenApiEnabled)
        {
            <a class="d-block mb-2" href="/api/index.html">API description</a>
        }

@*        <a class="d-block mb-2" href="/.well-known/openid-configuration">OpenId configuration</a>
*@
    </div>
    <div class="text-info position-absolute" style="right: 15px; bottom: 15px; color: navy !important;">
        
    </div>
</div>